{
  "name": "dsp_scim_handler",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/hana-client": "^2",
    "@sap/xsenv": "^6.0.0",
    "@sap/xssec": "^4.9.1",
    "axios": "^1.12.2",
    "express": "^4",
    "https": "^1.0.0",
    "node-fetch": "^3.3.2",
    "node-jose": "^2.2.0",
    "undici": "^7.16.0",
    "util": "^0.12.5"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.13.0",
    "@cap-js/sqlite": "^2.0.2",
    "@sap/hdi-deploy": "^5"
  },
  "scripts": {
    "start": "cds-serve",
    "start:prod": "cds-serve --production",
    "deploy:hana": "cds deploy --to hana"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "hana",
        "[production]": {
          "kind": "hana"
        },
        "auth": {
          "kind": "mocked",
          "[production]": {
            "kind": "xsuaa"
          }
        }
      }
    }
  }
}